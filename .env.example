# Live Mode for "Ask the Historian". Optional — the whole app runs in Demo Mode
# (cached, grounded, offline) without it. Set this in Vercel → Project → Settings
# → Environment Variables to enable grounded generation via Claude.
ANTHROPIC_API_KEY=
